const fs = require('fs'); let code = fs.readFileSync('controllers/loans.js', 'utf8'); const fn = \const normalizeLoan = (loan) => { let obj = loan; if (loan && typeof loan.toObject === 'function') obj = loan.toObject(); if (obj && obj.amountPaise === undefined && obj.amount !== undefined) obj.amountPaise = Math.round(obj.amount * 100); return obj; };\; if (!code.includes('const normalizeLoan')) { code = code.replace(/const Loan = require\\('\\.\\.\\/models\\/Loan'\\);/, 'const Loan = require(\\'../models/Loan\\');\\n' + fn); fs.writeFileSync('controllers/loans.js', code); }

const mongoose = require('mongoose'); const User = require('./models/User'); const ChitFund = require('./models/ChitFund'); require('dotenv').config(); mongoose.connect(process.env.MONGODB_URI).then(async () => { const chit = await ChitFund.findOne({ totalMonths: 10 }).sort({_id: -1}); const owner = await User.findOne({ id: chit.owner }); console.log('Chit Owner ID:', chit.owner); console.log('Found User Name:', owner.firstName); console.log('User ID in DB:', owner.id); process.exit(0); });

const mongoose = require('mongoose'); const User = require('./models/User'); require('dotenv').config(); mongoose.connect(process.env.MONGODB_URI).then(async () => { const user = await User.findOne({ email: { $exists: true } }).sort({ createdAt: -1 }); console.log('Email:', user.email, 'isEmailVerified:', user.isEmailVerified); process.exit(0); });
